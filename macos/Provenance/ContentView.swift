import SwiftUI

struct ContentView: View {
    var body: some View {
        Color(nsColor: .windowBackgroundColor)
            .frame(minWidth: 640, minHeight: 420)
    }
}

#Preview {
    ContentView()
}
