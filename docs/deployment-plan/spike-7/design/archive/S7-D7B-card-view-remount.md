# S7-D7B — Remount Card on this view

**Kind:** Claude Design **child-board** slip (not the main design-system project)  
**Spike:** Provenencia Spike 7  
**Parent:** [`S7-D7-card-component.md`](S7-D7-card-component.md) (Card kit page — paste that on the **main** system only)  
**Implements later as:** PR **S7-13** (Swift cousins). This slip is design-board remount only.

Paste **this entire document** into **one** product view board. Do not paste it into the main design system. You cannot see the parent brief or the other view files — everything you need is below.

---

## Who you are

Match **this** board. Remount only your row. Ignore the others.

| If this board is | Remount these treatments onto kit **Card** |
| --- | --- |
| **Source detail** | Artifacts list (elevated); expanded primary-file tile (`sm` radius); dashed metadata suggestion row |
| **Subject fields** | Property list card; inspector card. **Not** the type-strip tiles |
| **Citation composer** | Raised Observation summary rows. Locked Connect rows may stay sunken or become Card sunken — do not invent a third chrome |
| **Source types** | Suggested-fields stack (`sm` radius) |
| **Onboarding Flow** | Identify contributor list; Choose-file empty-folder dashed **sunken** block; selected-project meta card |

If you cannot tell which board you are, stop and ask. Do not remount every row.

---

## Do this (in order)

Card already exists on the **main** Provenencia design system. You are not designing Card. You are not copying Card into this project.

1. **Delete this board’s local design-system cache.** Claude Design often keeps a stale pack; remounting against it will recreate a local Card.
2. **Refetch the latest bundle** from the main Provenencia design system. Do not continue until the fetched kit lists **Card**.
3. Confirm Card supports: tones card / raised / sunken; border solid or dashed; radius `md` (default) or `sm`; optional shallow elevation; optional padding. Titles stay **outside** the Card.
4. Replace every treatment in **your** row with an **instance of kit Card** (same tone / border / radius / elevation / padding that site already uses).
5. **Delete the local implementation** — no leftover custom fill, clip, hairline, or dashed overlay that duplicates Card.

If Card is still missing after refetch: delete this board’s cache and refetch again. If it is still missing, stop. Do **not** draw a replacement Card here.

---

## Do not

- Redesign this view, move sections, or change copy.
- Remount Evidence graph subject / bridge / palette cards (those are not Card).
- Turn type-strip tiles, file-choice cells, icon-picker cells, or full-bleed pane fills into Card.
- Invent header/footer slots on Card.
- Copy Card source into this board’s local design system.
