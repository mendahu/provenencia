# Aggregate workspace nav counts (archived)

**Done.** `GetWorkspaceNavCounts` opens the catalog once and returns every sidebar badge (including origin splits for vocabulary). Feature models still **publish** after mutate so a create/delete does not need a full recount.
