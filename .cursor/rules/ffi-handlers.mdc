---
description: FFI RPCs are a thin Call switch plus handlers package tests via runRPC
globs: api/ffi/**/*.go,api/proto/engine.proto
alwaysApply: false
---

# FFI handlers

Follow the project skill `.cursor/skills/add-ffi-handler/SKILL.md`.

- `dispatch.go` only routes to `api/ffi/handlers`. New RPCs get a handler file and `runRPC` table tests, not a `run` closure in the table.
- Failed `provenencia_call` returns protobuf `Error` in `out` (status 1), not UTF-8 English. Domain errors use `core/apperr` codes.
